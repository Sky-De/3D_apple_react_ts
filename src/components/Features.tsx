const Features = () => {
  return (
    <section className="h-full common-padding bg-zinc relative overflow-hidden">
      Features
      {/* add features of product here */}
    </section>
  );
};

export default Features;
